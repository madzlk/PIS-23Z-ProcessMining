import { Replacement } from "../replacement";
import { Env } from "../types";
export declare function unwrapSignalForImportMetaEnvEnv(options: {
    example: readonly string[];
} & ({
    env: Env;
    transformMode: "compile-time";
} | {
    transformMode: "runtime";
})): Replacement[];
